apiVersion: v1
kind: Service
metadata:
  name: {{ include "exter.fullname" . }}
  labels:
    {{- include "exter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exter.selectorLabels" . | nindent 4 }}
